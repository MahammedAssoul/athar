import 'package:flutter/material.dart';

/// Central color palette for the Athar app.
class AppColors {
  AppColors._();

  // Brand
  static const Color primary = Color(0xFF0E7C5B); // Trustworthy green
  static const Color primaryDark = Color(0xFF0A5F45);
  static const Color primaryLight = Color(0xFFE6F4EF);

  // Accent
  static const Color accent = Color(0xFFF2A33C); // Warm hope orange
  static const Color accentLight = Color(0xFFFFF3E0);

  // Semantic
  static const Color success = Color(0xFF2E9E6B);
  static const Color error = Color(0xFFD64545);
  static const Color warning = Color(0xFFE8A33D);

  // Neutrals
  static const Color background = Color(0xFFF7F9F8);
  static const Color surface = Colors.white;
  static const Color textPrimary = Color(0xFF1C2B26);
  static const Color textSecondary = Color(0xFF5C6B66);
  static const Color textMuted = Color(0xFF8A9893);
  static const Color border = Color(0xFFE4EAE7);
  static const Color divider = Color(0xFFEDF1EF);

  // Verified badge
  static const Color verified = Color(0xFF1D7FD4);
}
